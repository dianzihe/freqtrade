# 2026-06-11

## 安装 freqtrade 项目

- 在 `F:\source\freqtrade` 完成完整安装
- Python: 3.13.12，虚拟环境位于 `.venv/`
- 安装范围：核心依赖 + Hyperopt + FreqAI + 开发依赖（含 PyTorch、stable-baselines3）
- 安装方式：`pip install -e .`（editable 模式）
- 安装版本：freqtrade 2026.6-dev-890fd8044
- 激活命令：`.venv\Scripts\activate`

## 启动 freqtrade 项目（dry-run 模式）

- 生成配置文件：`user_data/config.json`，策略：`user_data/strategies/SampleStrategy.py`
- 交易所：Binance，配置代理 `http://127.0.0.1:7890`（国内网络必须）
- 解决了 Windows 下 aiodns DNS 解析失败问题：卸载 aiodns + pycares
- 禁用 WebSocket（`"enable_ws": false`），避免代理不支持 WS 的报错
- API Server：`http://127.0.0.1:8080`，账号 freqtrader / freqtrader123
- 日志：`user_data/logs/freqtrade.log`
- 启动命令：`.venv/Scripts/freqtrade trade --config user_data/config.json --strategy SampleStrategy --logfile user_data/logs/freqtrade.log`

## 安装 Freqtrade Web UI

- 运行 `freqtrade install-ui` 下载并安装 freqUI v3.0.2 到 `freqtrade/rpc/api_server/ui/`
- 安装后通过 API `/api/v1/stop` 优雅停止旧进程，再重新后台启动
- Web UI 地址：`http://127.0.0.1:8080`（登录账号：freqtrader / freqtrader123）
