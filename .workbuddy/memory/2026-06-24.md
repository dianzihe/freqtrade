# 2026-06-24 工作日志

## 批量回测完成

### 完成事项
- 创建回测配置文件：`user_data/backtest_config_1m.json` 和 `backtest_config_15m.json`
  - 包含代理配置（`http://127.0.0.1:7890`）
  - 修复了 `order_types` 配置格式（需用 `order_time_in_force` 而非 `order_types`）
- 编写 `batch_backtest.py`：批量回测脚本，自动处理币种分类和数据覆盖问题
- 编写 `generate_report.py`：解析回测日志生成 HTML 报告
- 完成全部 23 个策略 × 1m/15m 回测，生成 `backtest_report.html`

### 数据覆盖情况
- 1m 数据：18 个币种全有
- 15m 数据：仅 8 个币种（BTC, ETH, SOL, XRP, H, VELVET, BEAT, DN）
- `GateScalpMomentumStrategy` 和 `TrendFilteredGridStrategy` 需要 15m informative，已自动限制到有 15m 数据的币种

### 关键发现
- 回测时间范围只有 13 天（2026-06-08 ~ 2026-06-21），数据量偏少
- `TrendPyramidStrategy/1m` 表现最好，18 个币种全部有交易
- `ChanlunCenterBreakoutStrategy/15m` 在妖币上表现突出
-  many 缠论策略在 1m 上交易数为 0（不适应 1m 高频数据）

### 技术问题记录
- Windows 中文环境下 subprocess 默认用 GBK 解码，需用 `encoding='utf-8'` + `errors='replace'`
- 并行跑回测在 Windows 上有问题，改用串行方式
- freqtrade 回测时若币种无交易不会报错，需在脚本中解析输出判断
